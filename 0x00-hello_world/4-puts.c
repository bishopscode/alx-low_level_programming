#include <stdio.h>
   /**
    *main - Entry point
    *This function is the entry point of the program. It demonstrates the usage
    * of the `puts` function to print a string to the standard output.
    *Return: Always 0 (success)
    */
int main(void)
{
	/*using the 'put' function to print a string*/
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
